#!/usr/bin/env node

/**
 * Docker Performance Testing Script
 * Measures latency impact of containerized vs native deployment
 */

const WebSocket = require('ws');
const http = require('http');
const { performance } = require('perf_hooks');

class PerformanceTester {
    constructor() {
        this.results = {
            websocket: [],
            http: [],
            robotCommands: []
        };
        
        this.testConfig = {
            backendUrl: process.env.BACKEND_URL || 'http://localhost:3001',
            wsUrl: process.env.WS_URL || 'ws://localhost:3001',
            iterations: parseInt(process.env.TEST_ITERATIONS) || 50,
            concurrency: parseInt(process.env.TEST_CONCURRENCY) || 5
        };
    }
    
    /**
     * Test WebSocket latency
     */
    async testWebSocketLatency() {
        console.log('\n📡 Testing WebSocket Latency...');
        console.log('=====================================');
        
        const latencies = [];
        
        for (let i = 0; i < this.testConfig.iterations; i++) {
            try {
                const latency = await this.measureWebSocketPing();
                latencies.push(latency);
                
                if (i % 10 === 0) {
                    process.stdout.write(`Progress: ${i}/${this.testConfig.iterations} - Current: ${latency.toFixed(2)}ms\\r`);
                }
                
                // Small delay between tests
                await new Promise(resolve => setTimeout(resolve, 100));
                
            } catch (error) {
                console.error(`WebSocket test ${i + 1} failed:`, error.message);
            }
        }
        
        this.results.websocket = latencies;
        this.printLatencyStats('WebSocket', latencies);\n    }\n    \n    /**\n     * Measure single WebSocket ping\n     */\n    measureWebSocketPing() {\n        return new Promise((resolve, reject) => {\n            const startTime = performance.now();\n            const ws = new WebSocket(this.testConfig.wsUrl + '/socket.io');\n            \n            const timeout = setTimeout(() => {\n                ws.terminate();\n                reject(new Error('WebSocket connection timeout'));\n            }, 5000);\n            \n            ws.on('open', () => {\n                const connectTime = performance.now();\n                \n                // Send ping message\n                ws.send(JSON.stringify({ type: 'ping', timestamp: Date.now() }));\n                \n                ws.on('message', (data) => {\n                    try {\n                        const response = JSON.parse(data.toString());\n                        if (response.type === 'pong') {\n                            const endTime = performance.now();\n                            const latency = endTime - startTime;\n                            \n                            clearTimeout(timeout);\n                            ws.close();\n                            resolve(latency);\n                        }\n                    } catch (error) {\n                        // Handle non-JSON messages (Socket.io handshake)\n                        const endTime = performance.now();\n                        const latency = endTime - startTime;\n                        \n                        clearTimeout(timeout);\n                        ws.close();\n                        resolve(latency);\n                    }\n                });\n            });\n            \n            ws.on('error', (error) => {\n                clearTimeout(timeout);\n                reject(error);\n            });\n        });\n    }\n    \n    /**\n     * Test HTTP API latency\n     */\n    async testHttpLatency() {\n        console.log('\\n🌐 Testing HTTP API Latency...');\n        console.log('==================================');\n        \n        const latencies = [];\n        \n        for (let i = 0; i < this.testConfig.iterations; i++) {\n            try {\n                const latency = await this.measureHttpRequest('/health');\n                latencies.push(latency);\n                \n                if (i % 10 === 0) {\n                    process.stdout.write(`Progress: ${i}/${this.testConfig.iterations} - Current: ${latency.toFixed(2)}ms\\r`);\n                }\n                \n                await new Promise(resolve => setTimeout(resolve, 50));\n                \n            } catch (error) {\n                console.error(`HTTP test ${i + 1} failed:`, error.message);\n            }\n        }\n        \n        this.results.http = latencies;\n        this.printLatencyStats('HTTP API', latencies);\n    }\n    \n    /**\n     * Measure single HTTP request\n     */\n    measureHttpRequest(endpoint) {\n        return new Promise((resolve, reject) => {\n            const startTime = performance.now();\n            const url = new URL(endpoint, this.testConfig.backendUrl);\n            \n            const req = http.get(url, (res) => {\n                let data = '';\n                \n                res.on('data', (chunk) => {\n                    data += chunk;\n                });\n                \n                res.on('end', () => {\n                    const endTime = performance.now();\n                    const latency = endTime - startTime;\n                    resolve(latency);\n                });\n            });\n            \n            req.on('error', (error) => {\n                reject(error);\n            });\n            \n            req.setTimeout(5000, () => {\n                req.destroy();\n                reject(new Error('Request timeout'));\n            });\n        });\n    }\n    \n    /**\n     * Test robot command latency\n     */\n    async testRobotCommandLatency() {\n        console.log('\\n🤖 Testing Robot Command Latency...');\n        console.log('====================================');\n        \n        const latencies = [];\n        const testCommands = [\n            { type: 'get_status' },\n            { type: 'move_forward', distance: 10 },\n            { type: 'turn_left', angle: 45 },\n            { type: 'get_sensor_data' }\n        ];\n        \n        for (let i = 0; i < this.testConfig.iterations; i++) {\n            try {\n                const command = testCommands[i % testCommands.length];\n                const latency = await this.measureRobotCommand(command);\n                latencies.push(latency);\n                \n                if (i % 10 === 0) {\n                    process.stdout.write(`Progress: ${i}/${this.testConfig.iterations} - Current: ${latency.toFixed(2)}ms\\r`);\n                }\n                \n                await new Promise(resolve => setTimeout(resolve, 200));\n                \n            } catch (error) {\n                console.error(`Robot command test ${i + 1} failed:`, error.message);\n            }\n        }\n        \n        this.results.robotCommands = latencies;\n        this.printLatencyStats('Robot Commands', latencies);\n    }\n    \n    /**\n     * Measure robot command latency\n     */\n    measureRobotCommand(command) {\n        return new Promise((resolve, reject) => {\n            const startTime = performance.now();\n            \n            // Simulate robot command via HTTP API\n            const postData = JSON.stringify(command);\n            const url = new URL('/api/robot/command', this.testConfig.backendUrl);\n            \n            const options = {\n                hostname: url.hostname,\n                port: url.port,\n                path: url.pathname,\n                method: 'POST',\n                headers: {\n                    'Content-Type': 'application/json',\n                    'Content-Length': Buffer.byteLength(postData)\n                }\n            };\n            \n            const req = http.request(options, (res) => {\n                let data = '';\n                \n                res.on('data', (chunk) => {\n                    data += chunk;\n                });\n                \n                res.on('end', () => {\n                    const endTime = performance.now();\n                    const latency = endTime - startTime;\n                    resolve(latency);\n                });\n            });\n            \n            req.on('error', (error) => {\n                reject(error);\n            });\n            \n            req.setTimeout(10000, () => {\n                req.destroy();\n                reject(new Error('Robot command timeout'));\n            });\n            \n            req.write(postData);\n            req.end();\n        });\n    }\n    \n    /**\n     * Print latency statistics\n     */\n    printLatencyStats(testType, latencies) {\n        if (latencies.length === 0) {\n            console.log(`\\n❌ No successful ${testType} measurements`);\n            return;\n        }\n        \n        const sorted = latencies.sort((a, b) => a - b);\n        const min = sorted[0];\n        const max = sorted[sorted.length - 1];\n        const avg = latencies.reduce((sum, val) => sum + val, 0) / latencies.length;\n        const median = sorted[Math.floor(sorted.length / 2)];\n        const p95 = sorted[Math.floor(sorted.length * 0.95)];\n        const p99 = sorted[Math.floor(sorted.length * 0.99)];\n        \n        console.log(`\\n📊 ${testType} Latency Statistics:`);\n        console.log(`   • Samples: ${latencies.length}`);\n        console.log(`   • Min: ${min.toFixed(2)}ms`);\n        console.log(`   • Average: ${avg.toFixed(2)}ms`);\n        console.log(`   • Median: ${median.toFixed(2)}ms`);\n        console.log(`   • 95th percentile: ${p95.toFixed(2)}ms`);\n        console.log(`   • 99th percentile: ${p99.toFixed(2)}ms`);\n        console.log(`   • Max: ${max.toFixed(2)}ms`);\n        \n        // Performance assessment\n        this.assessPerformance(testType, avg, p95);\n    }\n    \n    /**\n     * Assess performance against robot control requirements\n     */\n    assessPerformance(testType, avgLatency, p95Latency) {\n        const thresholds = {\n            'WebSocket': { good: 10, acceptable: 25, poor: 50 },\n            'HTTP API': { good: 20, acceptable: 50, poor: 100 },\n            'Robot Commands': { good: 30, acceptable: 100, poor: 200 }\n        };\n        \n        const threshold = thresholds[testType] || thresholds['HTTP API'];\n        \n        let assessment = '';\n        let emoji = '';\n        \n        if (p95Latency <= threshold.good) {\n            assessment = 'EXCELLENT';\n            emoji = '🟢';\n        } else if (p95Latency <= threshold.acceptable) {\n            assessment = 'ACCEPTABLE';\n            emoji = '🟡';\n        } else if (p95Latency <= threshold.poor) {\n            assessment = 'POOR';\n            emoji = '🟠';\n        } else {\n            assessment = 'UNACCEPTABLE';\n            emoji = '🔴';\n        }\n        \n        console.log(`   ${emoji} Performance: ${assessment} (95th percentile: ${p95Latency.toFixed(2)}ms)`);\n    }\n    \n    /**\n     * Generate performance report\n     */\n    generateReport() {\n        console.log('\\n📈 DOCKER PERFORMANCE ANALYSIS REPORT');\n        console.log('======================================');\n        \n        const timestamp = new Date().toISOString();\n        const environment = process.env.NODE_ENV || 'development';\n        \n        console.log(`\\n🕐 Test Completed: ${timestamp}`);\n        console.log(`🔧 Environment: ${environment}`);\n        console.log(`🎯 Target: ${this.testConfig.backendUrl}`);\n        console.log(`📊 Test Iterations: ${this.testConfig.iterations}`);\n        \n        // Calculate overall performance impact\n        const wsAvg = this.results.websocket.reduce((sum, val) => sum + val, 0) / this.results.websocket.length;\n        const httpAvg = this.results.http.reduce((sum, val) => sum + val, 0) / this.results.http.length;\n        const robotAvg = this.results.robotCommands.reduce((sum, val) => sum + val, 0) / this.results.robotCommands.length;\n        \n        console.log('\\n🎯 COMPETITION READINESS ASSESSMENT:');\n        \n        // Robot control assessment (most critical)\n        if (robotAvg < 100) {\n            console.log('   ✅ Robot Control: COMPETITION READY');\n            console.log(`      Average response time: ${robotAvg.toFixed(2)}ms (Target: <100ms)`);\n        } else {\n            console.log('   ❌ Robot Control: NEEDS OPTIMIZATION');\n            console.log(`      Average response time: ${robotAvg.toFixed(2)}ms (Target: <100ms)`);\n        }\n        \n        // Real-time communication assessment\n        if (wsAvg < 25) {\n            console.log('   ✅ Real-time Communication: EXCELLENT');\n            console.log(`      WebSocket latency: ${wsAvg.toFixed(2)}ms (Target: <25ms)`);\n        } else if (wsAvg < 50) {\n            console.log('   🟡 Real-time Communication: ACCEPTABLE');\n            console.log(`      WebSocket latency: ${wsAvg.toFixed(2)}ms (Target: <25ms)`);\n        } else {\n            console.log('   🟠 Real-time Communication: NEEDS IMPROVEMENT');\n            console.log(`      WebSocket latency: ${wsAvg.toFixed(2)}ms (Target: <25ms)`);\n        }\n        \n        // API performance assessment\n        if (httpAvg < 50) {\n            console.log('   ✅ API Performance: EXCELLENT');\n        } else if (httpAvg < 100) {\n            console.log('   🟡 API Performance: GOOD');\n        } else {\n            console.log('   🟠 API Performance: ACCEPTABLE');\n        }\n        \n        console.log('\\n💡 RECOMMENDATIONS:');\n        \n        if (robotAvg > 50) {\n            console.log('   • Consider using host networking mode for backend container');\n            console.log('   • Optimize WebSocket configuration for lower latency');\n        }\n        \n        if (wsAvg > 25) {\n            console.log('   • Enable WebSocket compression only if bandwidth is limited');\n            console.log('   • Consider dedicated network interface for robot communication');\n        }\n        \n        console.log('   • Monitor performance during actual competition conditions');\n        console.log('   • Test with multiple concurrent connections');\n        console.log('   • Validate performance under network congestion');\n        \n        console.log('\\n🏁 FINAL VERDICT:');\n        const overallReady = robotAvg < 100 && wsAvg < 50;\n        \n        if (overallReady) {\n            console.log('   🎉 DOCKER DEPLOYMENT IS COMPETITION READY!');\n            console.log('   🚀 Performance overhead is within acceptable limits');\n            console.log('   ✅ Proceed with containerized deployment');\n        } else {\n            console.log('   ⚠️  PERFORMANCE OPTIMIZATION NEEDED');\n            console.log('   🔧 Consider native deployment for critical components');\n            console.log('   📊 Repeat testing after optimizations');\n        }\n    }\n    \n    /**\n     * Run all performance tests\n     */\n    async runAllTests() {\n        console.log('🧪 DOCKER PERFORMANCE TESTING SUITE');\n        console.log('=====================================');\n        console.log(`Target Backend: ${this.testConfig.backendUrl}`);\n        console.log(`WebSocket URL: ${this.testConfig.wsUrl}`);\n        console.log(`Test Iterations: ${this.testConfig.iterations}`);\n        console.log('');\n        \n        try {\n            await this.testHttpLatency();\n            await this.testWebSocketLatency();\n            await this.testRobotCommandLatency();\n            \n            this.generateReport();\n            \n        } catch (error) {\n            console.error('\\n❌ Performance testing failed:', error.message);\n            process.exit(1);\n        }\n    }\n}\n\n// Run tests if called directly\nif (require.main === module) {\n    const tester = new PerformanceTester();\n    tester.runAllTests()\n        .then(() => {\n            console.log('\\n✅ Performance testing completed successfully');\n            process.exit(0);\n        })\n        .catch((error) => {\n            console.error('\\n💥 Performance testing failed:', error.message);\n            process.exit(1);\n        });\n}\n\nmodule.exports = PerformanceTester;\n"