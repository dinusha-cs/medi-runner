# Line-Following Robot Flask Server – Python dependencies
# Install: pip install -r requirements.txt

flask>=3.0
flask-cors>=4.0
numpy>=1.24
opencv-python-headless>=4.8    # headless = no GUI deps (server-friendly)

# Optional – only needed on Raspberry Pi
# RPi.GPIO>=0.7
# picamera2>=0.3
